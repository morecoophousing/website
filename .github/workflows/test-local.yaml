name: test-local

on: 
  workflow_dispatch:

jobs:
  test-local:
    environment: basic
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo $KEY > foo.txt
          ls -l *
          cat foo.txt

        env: 
          KEY: ${{ secrets.FAKE_SSH_KEY }}
